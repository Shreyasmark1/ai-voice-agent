NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

DATABASE_URL=

LLM_API_KEY=
LLM_API_ENDPOINT=https://openrouter.ai/api/v1
LLM_MODEL=poolside/laguna-xs-2.1:free

# Must match VOICE_AGENT_SECRET in the voice-agent project
VOICE_AGENT_SECRET=change-me-to-a-random-string
VOICE_AGENT_URL=ws://localhost:8765

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# 256-bit key used to encrypt stored refresh tokens at rest. Must be exactly 32 bytes (or 64 hex chars) and stable across restarts.
GOOGLE_CALENDAR_ENCRYPTION_KEY=